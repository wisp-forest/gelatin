moduleDependencies(project, [
    'dye-registry',
    'common'
])

dependencies {
    include("maven.modrinth:better-beds:${project.better_beds}")

    modCompileOnly ("maven.modrinth:diagonal-windows:v8.0.1-1.20.1-Fabric")
}

processResources {
    duplicatesStrategy = DuplicatesStrategy.WARN
}